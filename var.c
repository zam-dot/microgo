
#include <stdio.h>
void say(char *msg) { printf("%s\n", msg); }
int  main() {
    malloc(100);
    say("Hello");

    // Deferred statements
    say("See you");
    free((void *)(ptr));
    return 0;
}
